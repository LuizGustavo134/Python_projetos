import os
os.system
L1=["matr","nome","sal",]
while True:
        op= input('''o que deseja fazer?
          [1] incluir matricula
          [2] alterar matricula
          [3] excluir matricula
          [4] Pesquisar
          [5] relatório
          ''')
        if op not in["1","2","3","4","5"]:
            print("digite uma opção válida")
        matr = int(input ("Entre com a matricula: "))
        nome = input ("Entre com o nome: ")
        Cg = input ("Entre com o cargo: ")
        sal = float(input ("Entre com o salário: "))
        print=(i, db[0],db,[1])
        if matr not in op:
            op[matr]=[nome,sal]
        else:
            print ("Matricula já existe")
        for i, db in enumerate:  
            break